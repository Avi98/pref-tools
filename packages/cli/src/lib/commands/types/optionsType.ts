export type UploadOptionsType = {
  /** API key required */
  APIKey: string;
};

export type AssertOptionsType = {
  /** budgetFile based on which the assertion can be done. defaults to './budget.json' */
  budgetFile: string;
  /** @Todo */
};

export type BatchRunnerOptionsType = {
  /** @Todo */
};
